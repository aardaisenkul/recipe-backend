[build]
builder = "nixpacks"
buildCommand = "npm run build"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 100

[deploy.migrations]
command = "psql $DATABASE_URL -f src/db/schema.sql" 